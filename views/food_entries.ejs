<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Entries - Food Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/food-entries">
                <i class="bi bi-apple"></i> Food Tracker
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/food-entries">
                            <i class="bi bi-list-ul"></i> Food Entries
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/food-entries/create">
                            <i class="bi bi-plus-circle"></i> Add Entry
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h2 mb-0">Your Food Entries</h1>
            <a href="/food-entries/create" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i> Add New Entry
            </a>
        </div>
        
        <% if (foodEntries.length === 0) { %>
            <div class="alert alert-info text-center py-5">
                <i class="bi bi-info-circle fs-1 text-info mb-3"></i>
                <h4>No food entries found</h4>
                <p class="mb-3">Start tracking your meals by adding your first food entry.</p>
                <a href="/food-entries/create" class="btn btn-primary btn-lg">
                    <i class="bi bi-plus-circle"></i> Create Your First Entry
                </a>
            </div>
        <% } else { %>
            <div class="row">
                <% foodEntries.forEach(function(entry) { %>
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body">
                                <h5 class="card-title d-flex align-items-center">
                                    <i class="bi bi-circle-fill text-primary me-2" style="font-size: 0.5em;"></i>
                                    <%= entry.foodName %>
                                </h5>
                                <div class="row g-2 mb-2">
                                    <div class="col-6">
                                        <small class="text-muted d-block">Calories</small>
                                        <strong class="text-primary"><%= entry.calories %></strong>
                                    </div>
                                    <div class="col-6">
                                        <small class="text-muted d-block">Serving</small>
                                        <strong><%= entry.servingSize %> <%= entry.unit %></strong>
                                    </div>
                                </div>
                                <p class="card-text small text-muted mb-2">
                                    <i class="bi bi-calendar-event"></i> <%= new Date(entry.dateTime).toLocaleString() %><br>
                                    <i class="bi bi-clock"></i> <%= entry.meal %>
                                </p>
                                <% if (entry.tags.length > 0) { %>
                                    <div class="mb-3">
                                        <% entry.tags.forEach(function(tag) { %>
                                            <span class="badge bg-success me-1 mb-1"><%= tag %></span>
                                        <% }) %>
                                    </div>
                                <% } %>
                            </div>
                            <div class="card-footer bg-transparent">
                                <div class="btn-group w-100">
                                    <a href="/food-entries/edit/<%= entry.id %>" class="btn btn-outline-primary btn-sm">
                                        <i class="bi bi-pencil"></i> Edit
                                    </a>
                                    <a href="/food-entries/delete/<%= entry.id %>" class="btn btn-outline-danger btn-sm">
                                        <i class="bi bi-trash"></i> Delete
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>

    <footer class="bg-dark text-light mt-5 py-4">
        <div class="container text-center">
            <p class="mb-0">&copy; 2026 Food Tracker. Built with Express & Bootstrap.</p>
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
